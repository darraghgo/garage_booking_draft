


<html>
    <body>
    <div>

<% include partials/header.ejs %>
   
    
    <div class="table-wrapper">
        <% if (players.length > 0) {%>
            <table class="table table-hovered">
                <thead class="thead-dark">
                    <tr>
                        <th scope="col">ID</th>
                       
                        <th scope="col">First Name</th>
                        <th scope="col">Last Name</th>
                        <th scope="col">Booking</th>
                        <th scope="col">Number</th>
                        <th scope="col">Date</th>
                        <th scope="col">estimate</th>
                        <th scope="col">Assigned Mechanic</th>
                        <th scope="col">Change Mechanic</th>
                    </tr>
                </thead>
                <tbody>
                    <% players.forEach((player, index) => { %>
                        <tr>
                            <th scope="row"><%= player.id %></th>
                            
                            <td><%= player.first_name %></td>
                            <td><%= player.last_name %></td>
                            <td><%= player.booking %></td>
                            <td><%= player.number %></td>
                            <td><%= player.date %></td>
                            <td>€<%= player.estimate %></td>
                             <td><%= player.employee %></td>
                          
                            <td>
                                <form action="/applyEmp/<%= player.first_name %>" method="POST">
                              <select name="employee" >
    <option value="employee" disabled selected>select employee </option>
    <option value="ford" id = "employee">ford</option>
    <option value="john" id = "employee">john</option>
    <option value="mary" id = "employee">mary</option>
  </select>
                                <input type="submit">
                                </form>
                                 </td>
                            <td> <a href="/editBooking/<%= player.id %>" target="_blank" rel="noopener" class="btn btn-sm btn-success">Edit booking</a>  <a href="/bill/<%= player.id %>" target="_blank" rel="noopener" class="btn btn-md btn-info"> Bill</a></td> 
                             
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        <% } else { %>
            <p class="text-center">No players found. Go <a href="/add" >here</a> to add players.</p>
        <% } %>
    </div>
</div>
        
      
</body>
</html>
